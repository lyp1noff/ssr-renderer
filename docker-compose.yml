version: '3'
services:
  redis:
    image: redis:alpine
    container_name: redis
    ports:
      - "127.0.0.1:6379:6379"

  ssr-server:
    build: .
    container_name: ssr-server
    ports:
      - "127.0.0.1:3000:3000"
    depends_on:
      - redis
    environment:
      - REDIS_HOST=redis
